# requirements.txt
# ---- GPU-accelerated Torch stack (CUDA 12.1 wheels via --extra-index-url) ----
torch==2.4.1
torchaudio==2.4.1

# ---- Serverless runtime ----
runpod==1.6.0

# ---- Audio + utilities ----
ffmpeg-python==0.2.0
numpy>=1.26
soundfile>=0.12

# ---- Chatterbox TTS libs ----
chatterbox-tts==0.1.0
